const seed=[{id:1,text:'Confío en mí y en todo lo que soy capaz de lograr.',category:'Motivación',bg:'dawn'},{id:2,text:'No necesito tener todo resuelto para seguir avanzando.',category:'Amor propio',bg:'ocean'},{id:3,text:'Mi constancia transforma mis sueños en resultados.',category:'Éxito',bg:'night'},{id:4,text:'Hoy elijo agradecer lo que ya está floreciendo.',category:'Gratitud',bg:'forest'},{id:5,text:'Soy suficiente incluso mientras sigo creciendo.',category:'Amor propio',bg:'rose'},{id:6,text:'Respiro profundo. Estoy aquí. Estoy a salvo.',category:'Tranquilidad',bg:'ocean'},{id:7,text:'La disciplina de hoy crea la libertad de mañana.',category:'Disciplina',bg:'night'},{id:8,text:'Merezco relaciones que se sientan recíprocas y tranquilas.',category:'Relaciones',bg:'rose'}];
const cats=[['Todas','✦'],['Motivación','↗'],['Amor propio','♡'],['Éxito','◇'],['Tranquilidad','≈'],['Gratitud','☼'],['Disciplina','△'],['Relaciones','∞'],['Mis frases','＋']];const gradients={dawn:['#c58f82','#536b79'],ocean:['#668c99','#263d4a'],forest:['#708276','#293b35'],night:['#4b526d','#171b2a'],rose:['#c68d96','#745d70']};const cssGradient=k=>`linear-gradient(145deg,${gradients[k][0]},${gradients[k][1]})`;const fonts={serif:'Georgia,serif',sans:'Arial,sans-serif',soft:'cursive',classic:'Times New Roman,serif',typewriter:'Courier New,monospace',rounded:'Trebuchet MS,sans-serif',bold:'Impact,Arial Black,sans-serif'};const $=id=>document.getElementById(id);let quotes=JSON.parse(localStorage.getItem('alma_quotes')||'null')||seed;let favorites=JSON.parse(localStorage.getItem('alma_favs')||'[]');let prefs=Object.assign({bg:'dawn',font:'serif',color:'#ffffff',size:42,align:'left'},JSON.parse(localStorage.getItem('alma_prefs')||'null')||{});let category='Todas',index=0,imageData='';const filtered=()=>category==='Todas'?quotes:category==='Mis frases'?quotes.filter(q=>q.custom):quotes.filter(q=>q.category===category);const save=()=>{localStorage.setItem('alma_quotes',JSON.stringify(quotes));localStorage.setItem('alma_favs',JSON.stringify(favorites));localStorage.setItem('alma_prefs',JSON.stringify(prefs))};function toast(t){$('toast').textContent=t;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),1800)}
function current(){return filtered()[index]}function renderQuote(direction=0){let list=filtered();if(!list.length){category='Todas';list=filtered()}index=(index+list.length)%list.length;const q=list[index];$('quoteText').textContent=q.text;$('quoteCategory').textContent=q.category;$('categoryLabel').textContent=category==='Todas'?'Para ti':category;$('counter').textContent=`${String(index+1).padStart(2,'0')} / ${String(list.length).padStart(2,'0')}`;$('progressBar').style.width=`${((index+1)/list.length)*100}%`;$('favoriteBtn').textContent=favorites.includes(q.id)?'♥':'♡';$('favoriteBtn').classList.toggle('liked',favorites.includes(q.id));$('hero').style.background=q.image?`linear-gradient(#0002,#0003),url(${q.image}) center/cover`:cssGradient(q.bg||prefs.bg);Object.assign($('quoteText').style,{color:prefs.color,fontFamily:fonts[prefs.font],fontSize:prefs.size+'px',textAlign:prefs.align});if(direction)$('quoteText').animate([{opacity:0,transform:`translateY(${direction*22}px)`},{opacity:1,transform:'translateY(0)'}],{duration:280})}function show(name){document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));$(name).classList.add('active');document.querySelectorAll('.bottom button').forEach(b=>b.classList.toggle('selected',b.dataset.open===name));if(name==='favorites')renderFavorites()}
function renderCategories(){$('categoryGrid').innerHTML=cats.map(([c,i])=>{const n=c==='Todas'?quotes.length:c==='Mis frases'?quotes.filter(q=>q.custom).length:quotes.filter(q=>q.category===c).length;return `<button class="cat" data-cat="${c}"><em>${i}</em><b>${c}</b><small>${n} afirmaciones</small></button>`}).join('');document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{category=b.dataset.cat;index=0;renderQuote();show('home')});$('newCategory').innerHTML=cats.slice(1,-1).map(([c])=>`<option>${c}</option>`).join('')}function renderFavorites(){const list=quotes.filter(q=>favorites.includes(q.id));$('favoriteList').innerHTML=list.length?list.map(q=>`<article class="fav"><div class="fav-text"><strong>${q.text}</strong><small>${q.category}</small></div><button data-remove="${q.id}">♥</button></article>`).join(''):'<div class="empty">♡<br><br>Aquí aparecerán las afirmaciones que quieras guardar contigo.</div>';document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{favorites=favorites.filter(id=>String(id)!==b.dataset.remove);save();renderFavorites();renderQuote()})}document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>show(b.dataset.open));document.querySelectorAll('.back').forEach(b=>b.onclick=()=>show('home'));
$('favoriteBtn').onclick=()=>{const q=current();favorites=favorites.includes(q.id)?favorites.filter(id=>id!==q.id):[...favorites,q.id];save();renderQuote();toast(favorites.includes(q.id)?'Guardada en favoritos':'Eliminada de favoritos')};
function roundRect(ctx,x,y,w,h,r){ctx.beginPath();ctx.roundRect(x,y,w,h,r);ctx.fill()}function wrap(ctx,text,max){const words=text.split(' '),lines=[];let line='';for(const w of words){const test=line?line+' '+w:w;if(ctx.measureText(test).width>max&&line){lines.push(line);line=w}else line=test}if(line)lines.push(line);return lines}async function makeShareImage(q){const c=document.createElement('canvas');c.width=1080;c.height=1350;const x=c.getContext('2d');if(q.image){const img=new Image();img.src=q.image;await img.decode();x.drawImage(img,0,0,c.width,c.height);const g=x.createLinearGradient(0,0,0,c.height);g.addColorStop(0,'rgba(0,0,0,.12)');g.addColorStop(1,'rgba(0,0,0,.65)');x.fillStyle=g;x.fillRect(0,0,c.width,c.height)}else{const cs=gradients[q.bg||prefs.bg];const g=x.createLinearGradient(0,0,c.width,c.height);g.addColorStop(0,cs[0]);g.addColorStop(1,cs[1]);x.fillStyle=g;x.fillRect(0,0,c.width,c.height)}x.fillStyle='rgba(255,255,255,.16)';x.roundRect(75,110,250,70,35);x.fill();x.fillStyle='#fff';x.font='26px Arial';x.fillText(q.category,105,154);x.fillStyle=prefs.color;x.textAlign=prefs.align;x.font=`${Math.round(prefs.size*1.75)}px ${prefs.font==='typewriter'?'Courier New':prefs.font==='classic'?'Times New Roman':prefs.font==='sans'||prefs.font==='rounded'?'Arial':'Georgia'}`;const max=850,lines=wrap(x,q.text,max),lh=Math.round(prefs.size*2.05),start=720-lines.length*lh/2;const px=prefs.align==='center'?540:prefs.align==='right'?965:115;lines.forEach((l,i)=>x.fillText(l,px,start+i*lh));x.textAlign='left';x.fillStyle='rgba(255,255,255,.82)';x.font='bold 28px Arial';x.fillText('ALMA',75,1260);return new Promise(r=>c.toBlob(r,'image/png',.95))}$('shareBtn').onclick=async()=>{const q=current();try{toast('Preparando imagen…');const blob=await makeShareImage(q);const file=new File([blob],'afirmacion-alma.png',{type:'image/png'});if(navigator.share&&navigator.canShare&&navigator.canShare({files:[file]})){await navigator.share({title:'Una afirmación para ti',text:q.text,files:[file]})}else{const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='afirmacion-alma.png';a.click();toast('Imagen guardada para compartir')}}catch(e){toast('No se pudo compartir')}};
let sy=0,sx=0;$('hero').addEventListener('touchstart',e=>{sy=e.touches[0].clientY;sx=e.touches[0].clientX},{passive:true});$('hero').addEventListener('touchend',e=>{const dy=e.changedTouches[0].clientY-sy,dx=e.changedTouches[0].clientX-sx;if(Math.abs(dy)>55&&Math.abs(dy)>Math.abs(dx)){index+=dy<0?1:-1;renderQuote(dy<0?1:-1)}},{passive:true});$('hero').addEventListener('click',e=>{if(e.target.closest('button'))return;if(e.clientY>$('hero').clientHeight*.35){index++;renderQuote(1)}});$('newImage').onchange=e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{imageData=r.result;$('imagePreview').src=imageData;$('imagePreviewBox').style.display='block'};r.readAsDataURL(f)};$('saveBtn').onclick=()=>{const text=$('newText').value.trim();if(!text)return toast('Escribe tu afirmación');quotes.push({id:Date.now(),text,category:$('newCategory').value,image:imageData,custom:true,bg:prefs.bg});save();$('newText').value='';$('newImage').value='';$('imagePreviewBox').style.display='none';imageData='';renderCategories();category='Mis frases';index=filtered().length-1;renderQuote();show('home');toast('Tu afirmación está lista')};
function personalize(){prefs={bg:$('backgroundSelect').value,font:$('fontSelect').value,color:$('colorPicker').value,size:+$('sizeRange').value,align:document.querySelector('[data-align].active')?.dataset.align||prefs.align};$('settingsPreview').style.background=cssGradient(prefs.bg);const s=$('settingsPreview').querySelector('strong');Object.assign(s.style,{color:prefs.color,fontFamily:fonts[prefs.font],fontSize:Math.max(20,prefs.size*.64)+'px',textAlign:prefs.align});document.querySelectorAll('[data-color]').forEach(b=>b.classList.toggle('active',b.dataset.color.toLowerCase()===prefs.color.toLowerCase()));save();renderQuote()}$('backgroundSelect').value=prefs.bg;$('fontSelect').value=prefs.font;$('colorPicker').value=prefs.color;$('sizeRange').value=prefs.size;document.querySelectorAll('[data-align]').forEach(b=>{b.classList.toggle('active',b.dataset.align===prefs.align);b.onclick=()=>{document.querySelectorAll('[data-align]').forEach(x=>x.classList.remove('active'));b.classList.add('active');personalize()}});document.querySelectorAll('[data-color]').forEach(b=>b.onclick=()=>{$('colorPicker').value=b.dataset.color;personalize()});['backgroundSelect','fontSelect','colorPicker','sizeRange'].forEach(id=>$(id).oninput=personalize);renderCategories();renderQuote();personalize();